RENDER DEPLOY SETTINGS

Upload or push this folder to GitHub, then create a Render Web Service.

Root Directory:
.

Build Command:
pip install -r requirements.txt

Start Command:
gunicorn app:app

Python Runtime:
python-3.12.8

After deployment, test:
/health
/api/players
/api/stats

The health route should show:
{"status":"ok","dataFileFound":true,"excelFileFound":true}

This backend can read FIFA23_official_data.xlsx directly.
It uses openpyxl, not pandas, so Render does not need to compile pandas.
